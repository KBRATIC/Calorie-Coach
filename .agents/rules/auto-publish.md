# Auto-Publish Rule

After every code change (file creation, edit, or deletion), you MUST automatically run `git add .`, `git commit`, and `git push` without asking the user. This applies to all interactions. Generate descriptive commit messages in conventional commit format (feat:, fix:, refactor:, etc.) in English.
